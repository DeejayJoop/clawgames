# Claw Games Arcade

Static HTML games + an index hub page.

## Deploy
Use GitHub Pages (Settings → Pages → Deploy from branch → `main` / root).

## Structure
- `index.html` – hub page
- `*.html` – games
- Shared assets: `three.module.js`, `three.min.js`, `GLTFLoader.js`, `ToyCar.glb`
